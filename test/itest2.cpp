#include "br_index.hpp"

#include <cstdlib>
#include <iostream>

using namespace bri;
using namespace std;

int main(int argc, char** argv)
{
    if (arc != 5) { cout << "Invalid command line argument." << endl; exit(1); }

    
}